package ch.bbw.zork;/** * Repräsentiert einen Befehl im Spiel mit einem Befehlswort und optionalem zweiten Wort * * author: Michael Kolling version: 1.0 date: July 1999 * refactoring: Rinaldo Lanza, September 2020 */public class Command {	private String commandWord;  // Das Hauptbefehlswort (z.B. "go", "take")	private String secondWord;   // Optionales zweites Wort (z.B. "north", "sword")	// Konstruktor nur mit Befehlswort	public Command(String commandWord) {		this(commandWord, null);	}	// Konstruktor mit Befehlswort und zweitem Wort	public Command(String commandWord, String secondWord) {		this.commandWord = commandWord;		this.secondWord = secondWord;	}	// Gibt das Befehlswort zurück	public String getCommandWord() {		return commandWord;	}	// Gibt das zweite Wort zurück	public String getSecondWord() {		return secondWord;	}	// Prüft, ob der Befehl unbekannt ist	public boolean isUnknown() {		return (commandWord == null);	}	// Prüft, ob ein zweites Wort vorhanden ist	public boolean hasSecondWord() {		return (secondWord != null);	}}