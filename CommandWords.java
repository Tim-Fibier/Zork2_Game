package ch.bbw.zork;/** * author:  Michael Kolling, Version: 1.0, Date: July 1999 * refactoring: Rinaldo Lanza, September 2020 */import java.util.Arrays;import java.util.List;public class CommandWords {	// Liste aller gültigen Befehle im Spiel	private List<String> validCommands = Arrays.asList(			"go", "quit", "help", "inventory", "answer", "map", "take", "drop", "back"	);	// Prüft, ob ein Befehl gültig ist	public boolean isCommand(String commandWord) {		return validCommands.stream()				.filter( c -> c.equals(commandWord) )				.count()>0;	}	// Gibt alle gültigen Befehle als String zurück	public String showAll() {		return String.join(" ", validCommands);	}}